
/*export function getStatusBgClass(status: GYM_STATUS | string): string {
  const statusMap: Record<GYM_STATUS, string> = {
    [GYM_STATUS.ACTIVE]: 'bg-green-100 text-green-800',
    [GYM_STATUS.INACTIVE]: 'bg-gray-100 text-gray-800',
    [GYM_STATUS.PENDING]: 'bg-yellow-100 text-yellow-800',
    [GYM_STATUS.SUSPENDED]: 'bg-red-100 text-red-800',
  };

  return statusMap[status as GYM_STATUS] || 'bg-gray-100 text-gray-800';
}

export function getStatusLabel(status: GYM_STATUS | string): string {
  const labelMap: Record<GYM_STATUS, string> = {
    [GYM_STATUS.ACTIVE]: 'Actif',
    [GYM_STATUS.INACTIVE]: 'Inactif',
    [GYM_STATUS.PENDING]: 'En attente',
    [GYM_STATUS.SUSPENDED]: 'Suspendu',
  };

  return labelMap[status as GYM_STATUS] || status;
}*/
