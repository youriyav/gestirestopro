export const environment = {
  production: true,
  apiUrl: 'https://api.gestirestopro.com',
  // Wildcard domain the public menu is served on (landing), one subdomain per restaurant slug.
  publicMenuDomain: 'gestirestopro.com'
};
