export const environment = {
  production: false,
  apiUrl: 'http://localhost:3333',
  // No real subdomain locally — landing's dev server, read via ?slug= instead.
  publicMenuDomain: 'localhost:4300'
};
